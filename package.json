{
  "name": "tubby-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "build:tubby": "npm run build --workspace=packages/tubby",
    "build:tubby-react": "npm run build --workspace=packages/tubby-react"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Ludows/tubby.git"
  }
}
